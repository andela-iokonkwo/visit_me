<div class="container">

  <p id="notice"><%= notice %></p>

  <h1>All Meetings</h1>

  <div id="events_calendar">
    <%= render partial: "meeting_calendar", locals: {meetings: @meetings} %>
  </div>

  <br>

  <%= link_to 'New Meeting', new_meeting_path %>
</div>
